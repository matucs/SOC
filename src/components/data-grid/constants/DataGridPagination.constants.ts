import { tokens } from '../../../theme/tokens';
import { BUTTON_VARIANT_GHOST, BUTTON_SIZE_SM } from '../../ui/Button/Button.constants';

export const BTN_PREV = '←';
export const BTN_NEXT = '→';

export { BUTTON_VARIANT_GHOST as PAGE_BUTTON_VARIANT };
export { BUTTON_SIZE_SM as PAGE_BUTTON_SIZE };

export const FIRST_PAGE_INDEX = 0;

export const PAGINATION_BORDER_COLOR = tokens.border;
export const PAGINATION_SELECT_BG = tokens.bgSurface;
