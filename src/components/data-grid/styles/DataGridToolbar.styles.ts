import { tokens } from '../../../theme/tokens';

export const toolbarBarClass =
  'flex items-center justify-between gap-2 px-2 py-2 sm:px-3 border-b shrink-0';

export const toolbarBarStyle = { borderColor: tokens.border };
