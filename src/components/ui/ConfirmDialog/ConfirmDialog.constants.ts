export const DEFAULT_TITLE_ID = 'confirm-dialog-title';

export const CONFIRM_DIALOG_FOCUS_DELAY_MS = 50;

export const CONFIRM_DIALOG_PORTAL_TARGET = document.body;

export const Key = {
  ESCAPE: 'Escape',
  KEYDOWN: 'keydown',
} as const;

export const CONFIRM_DIALOG_KEYDOWN_CAPTURE = true;
