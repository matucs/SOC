import { tokens } from '../../../theme/tokens';

export const GROUP_HEADER_FOCUS = -1;

export const GROUP_ACCENT_COLOR = tokens.accent;
export const GROUP_BORDER_COLOR = tokens.border;
export const GROUP_COUNT_BADGE_BG = 'rgba(0,212,255,0.08)';
export const GROUP_COUNT_BADGE_BORDER = '1px solid rgba(0,212,255,0.2)';
export const GROUP_LINE_WIDTH = '1px';
export const GROUP_LINE_GRADIENT =
  `linear-gradient(to bottom, ${tokens.border} 0%, transparent 100%)`;
