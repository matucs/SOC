/** Minimum delay between simulated inbound events. */
export const SIMULATED_FEED_MIN_INTERVAL_MS = 12_000;

/** Maximum delay between simulated inbound events. */
export const SIMULATED_FEED_MAX_INTERVAL_MS = 28_000;
